# This file is only for notes. It is not meant to be executed.
# To build and run files, I do this.

docker build -t qiskit_alt_base  --rm=false -f Dockerfile0 .
docker run -it qiskit_alt_base /bin/bash

# Here are commands used to build images. These could potentially
# be executed to build a series of images.
docker build -t qiskit_alt_base  --rm=false -f Dockerfile0 .
docker build -t qiskit_alt_base1  --rm=false -f Dockerfile1 .
docker build -t qiskit_alt_2  --rm=false -f Dockerfile2 .
docker build -t qiskit_alt_3  --rm=false -f Dockerfile3 .
